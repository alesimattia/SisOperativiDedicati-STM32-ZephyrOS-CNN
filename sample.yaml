sample:
  description: TensorFlow Micro Person Detector
  name: person_detector
common:
  tags: tensorflow
  modules:
    - tflite-micro
  harness: console
  harness_config:
    type: multi_line
    ordered: false
